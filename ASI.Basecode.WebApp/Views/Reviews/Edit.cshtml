@model ASI.Basecode.Services.ServiceModels.ReviewViewModel

@{
    ViewData["Title"] = "Edit Review";
}

<h2>Edit Review</h2>

<form asp-action="Edit" method="post" enctype="multipart/form-data">
    <input type="hidden" asp-for="ReviewId" />
    <input type="hidden" asp-for="UploadDate" />

    <div class="form-group">
        <label asp-for="BookId" class="control-label">Book Name</label>
        <input asp-for="BookId" class="form-control"></input>
        <span asp-validation-for="BookId" class="text-danger"></span>
    </div>sayo
    <div class="form-group">
        <label asp-for="Rating" class="control-label">Rating</label>
        <input asp-for="Rating" type="number" step="0.1" min="0" max="5" class="form-control" />
        <span asp-validation-for="Rating" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Comment" class="control-label">Comment</label>
        <textarea asp-for="Comment" class="form-control"></textarea>
        <span asp-validation-for="Comment" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="UserId" class="control-label">User ID</label>
        <input asp-for="UserId" class="form-control" readonly value="@User.Identity.Name" />
        <span asp-validation-for="UserId" class="text-danger"></span>
    </div>

  @*   <div class="form-group">
        <label asp-for="ReviewImage" class="control-label">Review Image</label>
        @if (!string.IsNullOrEmpty(Model.ReviewImage))
        {
            <div>
                <img src="@Model.ReviewImage" alt="Current Review Image" style="max-width: 200px; margin-bottom: 10px;" />
                <div>
                    <input type="checkbox" name="RemoveImage" id="removeImage" />
                    <label for="removeImage">Remove current image</label>
                </div>
            </div>
        }
        <input type="file" name="ReviewImageFile" class="form-control" accept="image/*" />
        <span asp-validation-for="ReviewImage" class="text-danger"></span>
    </div> *@

    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>

@* @section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} *@